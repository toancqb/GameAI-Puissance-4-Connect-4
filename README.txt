To run the program:

>@: python3 game.py


*) Need the 'termcolor' module and python 3
*) The best performance is run in Linux systems

*) To install 'termcolor' in ubuntu/debian:

>@: sudo apt install python3-pip
>@: pip3 install termcolor

